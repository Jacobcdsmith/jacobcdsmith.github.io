# Monologue Compilation Book — Python dependencies.
# Install with:  pip install -r scripts/book/requirements.txt
#
# These are the runtime deps for the `npm run build:book` pipeline
# (parse → redact → cluster → typeset → PDF).

scikit-learn>=1.3.0
nltk>=3.8.0
reportlab>=4.0.0
pillow>=10.0.0
numpy>=1.24.0
